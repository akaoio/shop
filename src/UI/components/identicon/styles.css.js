import { css } from "/core/UI.js"

export const styles = css`
    :host {
        .identicon {
            width: 100%;
            aspect-radio: 1 / 1;
            display: block;
            color: currentColor;
        }
    }
`

export default styles
